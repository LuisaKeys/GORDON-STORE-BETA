@model IEnumerable<GORDON_STORE_BETA.Models.Estudio>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Listagem de Estudios";
}

<!-- Mensagem de exclusão -->
@if (@TempData["Message"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["Message"]
    </div>
}

<main>
    <div class="tabelainteira">
        <div class="headertabela">
            RELAÇÃO DE ESTUDIOS REGISTRADOS
        </div>
        <div class="itenstabela">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>
                            @Html.DisplayNameFor(model => model.Nome)
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.EstudioId)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Nome)
                            </td>
                            <td>
                                @Html.ActionLink("Alterar", "Edit", new { id = item.EstudioId }) |
                                @Html.ActionLink("Detalhes", "Details", new { id = item.EstudioId }) |
                                @Html.ActionLink("Eliminar", "Delete", new { id = item.EstudioId })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="panel-footer">
            @Html.ActionLink("Registrar um novo Produto", "Create", null,
            new { @class = "btn-adicionar" })
        </div>
    </div>
</main>

@section styles{
    <link href="@Url.Content("~/content/DataTables/css/dataTables.bootstrap.css")" rel="stylesheet" />
}
@section ScriptPage{
    <script src="@Url.Content("~/scripts/DataTables/jquery.dataTables.js")"></script>
    <script src="@Url.Content("~/scripts/DataTables/dataTables.bootstrap.js")"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            $('.table').dataTable();
        });
        $('li').removeClass("active");
        $('#liEstudios').addClass("active");
    </script>
}


