@model GORDON_STORE_BETA.Models.Estudio

@{ Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Registrando um NOVO ESTUDIO"; }

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <main>
        <div class="tabelainteira">
            <div class="headertabela">
                REGISTRO DE UM NOVO ESTUDIO
            </div>
            <div class="panel-body">
                <div class="form-horizontal">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        @Html.LabelFor(model => model.Nome, htmlAttributes:
                        new { @class = "control-label col-md-2" })
                        <div class="col-md-10">
                            @Html.EditorFor(model => model.Nome, new
                            {
                                htmlAttributes =
                            new { @class = "form-control" }
                            })
                            @Html.ValidationMessageFor(model => model.Nome, "",
                            new { @class = "text-danger" })
                        </div>
                    </div>
                </div>
                <div class="panel-footer">
                    <div class="col-md-offset-2">
                        <input type="submit" value="Adicionar Estudio" class="btn" />
                        @Html.ActionLink("Retornar para a listagem de ESTUDIOS", "Index", null,
                        new { @class = "btn btn-retornar" })
                    </div>
                    <br />
                    <br />
                </div>
            </div>
        </div>
    </main>
}

@section ScriptPage {
    <script type="text/javascript">
        $(document).ready(function () {
            $('li').removeClass("active");
            $('#liProdutos').addClass("active");
        });
    </script>
}